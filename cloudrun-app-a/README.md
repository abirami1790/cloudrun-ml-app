# Cloud Run App A (Gateway)

This FastAPI app acts as the gateway. It verifies Firebase ID tokens from the frontend, fetches Google-signed identity tokens, and securely routes requests to App B, C, and D.
